import React from 'react'

export const metadata = {
	// title: "About Page"
	// if not needed template
	title: {
		absolute: "absolute about page"
	}
}
const About = () => {
	return (
		<div>About Us</div>
	)
}

export default About